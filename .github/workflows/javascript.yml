name: JavaScript Build and Test

on:
  push:
    branches: [ main ]
    paths:
      - 'leetcode-javascript/**'
      - '.github/workflows/javascript.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'leetcode-javascript/**'
      - '.github/workflows/javascript.yml'

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: leetcode-javascript

    steps:
    - uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20.x'
        cache: 'npm'
        cache-dependency-path: leetcode-javascript/package-lock.json

    - name: Install dependencies
      run: npm ci

    - name: Run tests
      run: npm test
